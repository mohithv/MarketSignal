# Leave empty in dev to use Vite proxy (see vite.config.ts).
# For production or when API runs elsewhere, set the full origin, e.g.:
# VITE_API_URL=http://localhost:3000
VITE_API_URL=

# Optional: only if your API validates x-api-key (e.g. routes under /api).
VITE_API_KEY=
